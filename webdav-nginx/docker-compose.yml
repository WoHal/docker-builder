services:
  webdav:
    image: wohal/webdav-nginx
    container_name: webdav
    environment:
      - username=admin
      - password=password
    restart: always
    ports:
      - 8080:8080
    volumes:
      - ./data:/data